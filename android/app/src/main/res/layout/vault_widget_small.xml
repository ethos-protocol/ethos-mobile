<?xml version="1.0" encoding="utf-8"?>
<!-- Small widget layout: vault name + TTL countdown only (#247) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="#FF1C1C1E">

    <TextView
        android:id="@+id/widget_vault_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="14sp"
        android:textColor="#FFFFFFFF"
        android:text="—"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_ttl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#FFEBEBF5"
        android:layout_marginTop="4dp"
        android:text="TTL: —" />

    <!-- Stub views so RemoteViews.setTextViewText calls from updateWidget are safe
         on small layouts even though these fields are not displayed here. -->
    <TextView
        android:id="@+id/widget_balance"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:text="" />

    <TextView
        android:id="@+id/widget_beneficiary"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:text="" />

</LinearLayout>
